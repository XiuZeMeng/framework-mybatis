<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mxz.mybatis.mapper.ClientMapper">
    <select id="getByVO" resultType="Client">
        select id, username, password from t_client
        where username = #{username} and password = #{password}
    </select>

    <select id="getByMap" resultType="Client">
        select id, username, password from t_client
        where username = #{username} and password = #{password}
    </select>

    <select id="getByParam" resultType="Client">
        select id, username, password from t_client
        where username = #{username} and password = #{password}
    </select>
</mapper>